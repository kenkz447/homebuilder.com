import * as React from 'react'
const logo = require('../../../../../images/logo.png')
export const Logo = () => (
    <div className="brand-logo">
        <img className="brand-logo-image" src={logo} />
    </div>
)